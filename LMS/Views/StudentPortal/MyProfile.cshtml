@{
    ViewBag.Title = "Profile";
}

<h1 class="h3 mb-4">Profile</h1>
<div class="card border-0 shadow-sm">
    <div class="card-body">
        <p class="mb-2"><strong>Account email:</strong> @User.Identity.Name</p>
        <p class="text-muted small mb-0">Placeholder for editable profile fields (name, photo, etc.).</p>
    </div>
</div>
